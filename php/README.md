Image on Docker Hub: https://hub.docker.com/r/fnproject/php

## Using

```sh
docker run -it --rm fnproject/php php -v
```

## Building this image

```sh
ruby build.rb
```

Push:

```sh
docker push fnproject/php
```
